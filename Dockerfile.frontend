# 使用Node.js官方镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package.json文件
COPY package*.json ./

# 安装依赖（使用国内镜像加速）
RUN npm config set registry https://registry.npmmirror.com && \
    npm install

# 复制所有源代码
COPY . .

# 暴露端口
EXPOSE 8081

# 启动开发服务器
CMD ["npm", "run", "dev"]